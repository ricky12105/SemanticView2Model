# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Env / secrets
.env
config/mapping.yml

# Local-only semantic views (not for the shared repo)
semantic_view/P_and_C_semantic_view.sql

# Local-only environment-specific mapping (contains real Fabric/Snowflake IDs)
config/mapping.new_sf.yml

# Build output
out/
out_*/
dist/
build/

# IDE
.vscode/launch.json
