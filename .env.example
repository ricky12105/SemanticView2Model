# Fabric workspace + auth for `deploy` subcommand (override with CLI flags)
FABRIC_WORKSPACE_ID=00000000-0000-0000-0000-000000000000

# Service principal (optional — falls back to DefaultAzureCredential / az login)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
