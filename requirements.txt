pyyaml>=6.0
pydantic>=2.6
sqlglot>=25.0
azure-identity>=1.17
requests>=2.32
semantic-link-labs>=0.7
pytest>=8.0
# Optional: needed only for `sv2m sync --execute` (Snowflake DDL execution).
# Install via `pip install "sv2m[snowflake]"` or uncomment below.
# snowflake-connector-python>=3.7
